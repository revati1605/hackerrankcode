#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,rem,s=0,temp;
    scanf("%d", &n);
    temp=n;
    while(temp>0)
    {
       rem=temp%10;
       s=s+rem;
       temp=temp/10;
       
    }
    printf("%d\n",s);
    return 0;
}
